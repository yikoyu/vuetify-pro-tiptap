name: autofix.ci

on:
  push:
    branches: [master, develop]
  pull_request_target:
    branches: [master, develop]
    types:
      - opened
      - edited
      - synchronize

jobs:
  autofix:
    uses: yikoyu/workflows/.github/workflows/autofix.yml@main
    permissions:
      contents: write
      pull-requests: write
    with:
      command: pnpm lint:fix
